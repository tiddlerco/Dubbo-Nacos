server:
  port: 8091

spring:
  application:
    name: dubbo-user
  messages:
      # 国际化资源文件路径
    basename: i18n/messages
    encoding: UTF-8
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      username: nacos
      password: nacos